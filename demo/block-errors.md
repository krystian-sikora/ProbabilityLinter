# warning -- powtórzony jawny id
<block id="duplicate" />
<block id="duplicate" />

# error -- forma parowa (musi być self-closing)
<block id="paired"></block>

# poprawne (bez diagnostyki)
<block id="valid-1" />
<block id="valid-2" />

# error -- tag nie jest samozamykający (nagłówek bez />)
# (na końcu pliku, żeby nie „połknąć” późniejszego </block>)
<block id="unclosed">
